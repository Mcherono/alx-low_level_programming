#include "main.h"
/**
*_abs - compute the absolute value of an integer
*@n: the number to check
*Return: the abssolute value of the number
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

