#include "main.h"
#include <unistd.h>
/**
*_putchar - writes the chaacter to standard output
*@c: the character to print
*Return: on success return 1
*/
int _putchar(char c)
{
	return (write(1, &c, 1));
}
